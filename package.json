{
  "name": "cypress-automation-cadencerx",
  "version": "1.0.0",
  "description": "Cypress Cucumber Automation framework for Cadence Rx",
  "main": "index.js",
  "repository": "https://github.com/Cadence-Rx/cypress-automation-cadencerx.git",
  "author": "Latunji Asgill <lasgill@cadencerx.com>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.6",
    "@faker-js/faker": "^10.1.0",
    "cypress": "^15.3.0",
    "cypress-cucumber-attach-screenshots-to-failed-steps": "^1.0.0",
    "cypress-xpath": "^2.0.1",
    "multiple-cucumber-html-reporter": "^3.9.3",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/support/step_definitions/*.ts",
      "cypress/support/step_definitions/*.js "
    ],
    "html": {
      "enabled": true,
      "output": "cypress/reports/cucumber-html/cucumber-html-report.html"
    },
    "messages": {
      "enabled": true,
      "output": "cypress/reports/cucumber-ndjson/cucumber-messages.ndjson"
    },
    "json": {
      "enabled": true,
      "formatter": "cucumber-json-formatter",
      "output": "cypress/reports/cucumber-json/cucumber-report.json"
    }
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.1"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test": "cypress run",
    "full-regression-headed": "cypress run --headed --browser chrome --spec 'cypress/e2e/**/*.feature'",
    "full-regression": "cypress run --browser chrome --spec 'cypress/e2e/**/*.feature'",
    "contact-us-feature": "cypress run --browser chrome --spec 'cypress/e2e/**/Contact_Us.feature' || npm run posttest",
    "contact-us-tag": "cypress run -e TAGS=\"@contact-us\" --browser chrome || npm run posttest",
    "login-tag": "cypress run -e TAGS=\"@login\" --browser chrome || npm run posttest",
    "login-pass": "cypress run -e TAGS=\"@login\" --browser chrome",
    "login-and-smoke-tags": "cypress run -e TAGS=\"@login or @smoke\" --browser chrome || npm run posttest",
    "smoke-tag": "cypress run -e TAGS=\"@smoke\" --browser chrome || npm run posttest",
    "patient-tag": "cypress run -e TAGS=\"@patient\" --browser chrome",
    "regression-tag": "cypress run -e TAGS=\"@regression\" --browser chrome",
    "regression-not-smoke-tag": "cypress run -e TAGS=\"@regression and not @smoke\" --browser chrome",
    "posttest": "node cucumber-html-report.ts",
    "regression-report": "npm run regression-tag; npm run posttest"
  }
}
